define([
    "../../lib/service",
    "../../lib/validate"
],function(service, validateItem) {    
    return Backbone.Model.extend({
        defaults: {

        },
        initialize: function () {

        }
        
    });
});